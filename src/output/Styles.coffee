class Styles
  @Matrix: require('../lib/gl-matrix.js')

  constructor: (@engine) -> 
    @input   = @engine
    @output  = output

  position: (node, offsets) ->

  matrix: (node, offsets) ->

    
module.exports = Elements